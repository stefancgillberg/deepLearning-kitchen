# Copy to .env and fill in values.

# Model name from `ollama list` (must be pulled first). If unset, defaults to llama3.1:8b (see setup_ollama.sh).
# OLLAMA_CHAT_MODEL_ID=llama3.1:8b

# Optional: Ollama API base URL (omit for default http://127.0.0.1:11434).
# OLLAMA_HOST=http://127.0.0.1:11434
