# Copy to .env and fill in values.

# Required: model name as shown by `ollama list` (must be pulled first).
OLLAMA_CHAT_MODEL_ID=

# Optional: Ollama API base URL (omit for default http://127.0.0.1:11434).
# OLLAMA_HOST=http://127.0.0.1:11434
